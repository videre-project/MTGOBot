{
  "name": "project",
  "version": "1.0.0",
  "scripts": {
    "start:server": "pnpm run -r dev",
    "start:bot": "dotnet run --project src",
    "start": "pnpm run \"/^start:.*/\" --parallel",
    "dev": "pnpm start"
  },
  "engines": {
    "node": ">= 18",
    "pnpm": ">= 3"
  }
}